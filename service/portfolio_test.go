package service

import (
	"testing"
)

func TestCreatePortfolio(t *testing.T) {
	/*
		serv := NewService(&TestPostgres{})

		rw := httptest.NewRecorder()

		var body bytes.Buffer
		json.NewEncoder(&body).Encode(&CreatePortfolioRequest{Name: "test", Description: "test description"})
		req, _ := http.NewRequest("POST", "/portfolio", &body)
		serv.CreatePortfolio(rw, req)
		if rw.Code != http.StatusOK {
			t.Errorf("Failed to call CreatePortfolio Postgres function with error: %v", rw.Body)
		}
	*/
}
